# main.py

# Importa todo el ejemplo_module, pero con un alias 'em' para hacerlo mas facil.
# referencia.
import example_module as em

# Ahora podemos usar 'em' como el nombre del modulo para llamar a la funcion saludar.
print(em.greet())
